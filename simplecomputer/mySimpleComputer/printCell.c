#include "mySimpleComputer.h"

void
printCell ()
{
  // int val = 0;
  // sc_memoryGet (address, &val);

  // int col = address % COLS_IN_ROW;
  // int row = address / COLS_IN_ROW;

  // mt_gotoXY ((col * 6) + 2, (row + 1) + 1);

  // if (address == EDITING_CELL)
  //   {
  //     mt_setfgcolor (bg);
  //     mt_setbgcolor (fg);
  //   }
  // else
  //   {
  //     mt_setfgcolor (fg);
  //     mt_setbgcolor (bg);
  //   }

  // char sign_char = ((val >> 15) & 0x1) == 0 ? '+' : '-';
  // printf ("%c%04d\n", sign_char, val);

  // mt_setdefaultcolor ();
}
